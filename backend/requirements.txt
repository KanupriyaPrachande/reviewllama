# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.5.2

# --- ML / inference ---
# Loads the trained classifier from ml/training_simple/
joblib==1.4.2
scikit-learn==1.5.2

# --- Utilities ---
python-dotenv==1.0.1
httpx==0.27.2

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0

# --- Not needed for current scope, kept for reference if you later expand ---
# sqlalchemy==2.0.35       # storage is plain sqlite3 (see app/core/db.py) for this scope
# psycopg2-binary==2.9.9   # only needed if you move to Postgres later
# redis==5.1.0             # only needed if you add async job queues later
# celery==5.4.0
# torch / transformers / peft / accelerate / datasets  # only needed for LLM fine-tuning, not this scope
# PyGithub==2.4.0          # only needed for live GitHub webhook integration
# slack-sdk==3.33.1        # only needed for Slack alerts
